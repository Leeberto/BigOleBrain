# Decision Records

This folder holds durable decision records for BigOleBrain.

Use one file per decision. Each record should capture:

- date
- status
- context
- decision
- consequences

Use `docs/decisions/` when you want the why to remain searchable after code and roadmap items change.

# Build Logs

This folder holds implementation logs for completed or in-progress build slices.

Use one file per feature, sprint, or significant delivery slice. A build log should capture:

- what was built
- deviations from spec
- manual verification needs
- follow-up risks or notes

Use this folder instead of growing a single repo-wide log file.

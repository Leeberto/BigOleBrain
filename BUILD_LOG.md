# Build Log

This root file is now an archive pointer.

Implementation logs now live in `docs/builds/` as one file per feature, sprint, or significant build slice.

- Historical migrated log: `docs/builds/2026-04-06-thoughts-screen.md`
- New logs should be added under `docs/builds/`

Use:

- `docs/ROADMAP.md` for future priorities
- `docs/specs/` for implementation plans
- `docs/decisions/` for durable decisions
- `docs/builds/` for implementation history
